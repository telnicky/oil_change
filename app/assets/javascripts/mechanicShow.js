$(function () {
  var appointmentsView = new AppointmentView({collection: appointments});
  // insert appointmentsView.el into view
  //$('.mechanic-appointments-container').append(appointmentsView.el);
});